# Plaid Environment Configuration
PLAID_ENV=sandbox
PLAID_CLIENT_ID=
PLAID_SECRET=

# Encryption Key (32 bytes)
ENCRYPTION_KEY=

# Storage Mode: file or postgres
STORAGE_MODE=file

# Database URL (required if STORAGE_MODE=postgres)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# MCP Server Transport: stdio or streamable-http
MCP_TRANSPORT=stdio

# Optional: Balance cache TTL in seconds (default: 300)
# BALANCE_CACHE_TTL=300
